[project]
name = "todo-app"
version = "0.1.0"
description = "Todo In-Memory Python Console App"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rich>=13.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
